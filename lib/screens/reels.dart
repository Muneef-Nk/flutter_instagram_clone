import 'package:flutter/material.dart';

class ScreenReel extends StatelessWidget {
  const ScreenReel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('reel'),),
    );
  }
}
